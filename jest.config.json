{
  "collectCoverageFrom": [
    "<rootDir>/src/**/*.{ts,tsx}"
  ],
  "preset": "ts-jest",
  "testEnvironment": "jsdom",
  "testPathIgnorePatterns": [
    "<rootDir>/build/",
    "<rootDir>/dist/"
  ],
  "transform": {
    "^.+\\.tsx?$": "ts-jest"
  }
}
